package com.okta.quarkusclient;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeSecureResourceIT extends SecureResourceTest {

    // Execute the same tests but in native mode.
}